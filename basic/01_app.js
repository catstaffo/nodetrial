const names = require('./04_names');
const sayHi = require('./05_utils');
const data = require('./06_alternative');
//console.log(data);

// the below invokes the code if there is function
require('./07_invoke');
/*sayHi('susan');
sayHi(names.john);
sayHi(names.peter);*/
